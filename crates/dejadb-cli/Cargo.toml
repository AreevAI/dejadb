[package]
name = "dejadb"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Command-line interface for DejaDB, the embedded memory engine for AI agents."
readme = "README.md"
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords = ["memory", "cli", "ai-agents", "llm", "database"]
categories = ["command-line-utilities", "database-implementations"]

[[bin]]
name = "deja"
path = "src/main.rs"

[features]
# `deja --db postgres://…?schema=<name>` — the server-tier backend. Non-
# default: the published edge binary stays dependency-light.
postgres = ["dejadb-store/postgres"]

[dependencies]
dejadb-core = { path = "../dejadb-core", version = "1.1.1" }
dejadb-store = { path = "../dejadb-store", version = "1.1.1" }
dejadb-cal = { path = "../dejadb-cal", version = "1.1.1" }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
zeroize = "1"

dejadb-mcp = { path = "../dejadb-mcp", version = "1.1.1" }
dejadb-context = { path = "../dejadb-context", version = "1.1.1" }
dejadb-server = { path = "../dejadb-server", version = "1.1.1" }
deja-loop = { path = "../deja-loop", version = "1.1.1" }
dejadb-loop = { path = "../dejadb-loop", version = "1.1.1" }
dejadb-llm = { path = "../dejadb-llm", version = "1.1.1" }

[dev-dependencies]
tempfile = "3"
